<%= form_for :student, url: students_path do |s| %>

    <% if @student.errors.any? %>
        <div id="error_explanation">
          <h2>
            <%= pluralize(@student.errors.count, "error") %> prohibited
            this article from being saved:
          </h2>
          <ul>
            <% @student.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>
    <p>
      <%= s.label :name %><br>
      <%= s.text_field :name %>
    </p>

    <p>
      <%= s.label :last_name %><br>
      <%= s.text_field :last_name %>
    </p>

    <p>
      <%= s.label :person_id %><br>
      <%= s.text_field :person_id %>
    </p>

    <p>
      <%= s.label :birth_date %><br>
      <%= s.date_select :birth_date, :start_year => 1980%>
    </p>

    <p>
      <%= s.submit %>
    </p>
<% end %>