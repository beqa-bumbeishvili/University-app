<%= form_for :school, url: schools_path do |s| %>

    <% if @school.errors.any? %>
        <div id="error_explanation">
          <h2>
            <%= pluralize(@school.errors.count, "error") %> prohibited
            this article from being saved:
          </h2>
          <ul>
            <% @school.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>
    <p>
      <%= s.label :name %><br>
      <%= s.text_field :name %>
    </p>

    <p>
      <%= s.label :address %><br>
      <%= s.text_field :address %>
    </p>

    <p>
      <%= s.label :student_amount %><br>
      <%= s.text_area :student_amount %>
    </p>

    <p>
      <%= s.submit %>
    </p>
<% end %>